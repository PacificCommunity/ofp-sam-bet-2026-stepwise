# 11b Standard Fix8 control

Its `doitall.sh` rebuilds Step 11 from the initial inputs and applies standard
terminal fixed recruitment over eight periods (`pf398=1`, `pf400=8`). This gives a
standard-recruitment control with the same terminal span as an OPR `E2` basis.

It writes a fresh `11.par` and then `final.par`.
