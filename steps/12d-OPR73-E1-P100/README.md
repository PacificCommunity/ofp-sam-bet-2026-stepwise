# 12d Late-transfer OPR 73-E1, terminal penalty

After rebuilding Step 11 from the initial inputs, its `doitall.sh` uses fully
flexible annual OPR (`73-01-50-50`) with `E1`, then applies
`pf397=100` in the final refinement. With `E1`, the penalty operates with a
one-calendar-year terminal basis window.

This separates annual recruitment flexibility from the choice of terminal
penalty treatment.
