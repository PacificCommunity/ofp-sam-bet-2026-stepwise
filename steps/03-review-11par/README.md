# 03 Base 11.par Model 03

Independent model folder using the BET 2026 `11.par` input. This gives the
default task multiple model payloads while keeping the example fast. Edit
`config.env` or add `patch.R` to turn it into a real model variant.
