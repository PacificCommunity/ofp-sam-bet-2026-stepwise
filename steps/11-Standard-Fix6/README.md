# 11 Standard Fix6 reference

This is the PDH-rebuild Step 11 model copied unchanged from
`experiment/step12-pdh-termpen100-rebuild`.

Its `doitall.sh` runs from `bet.ini` to a fresh `11.par`. It is the
standard-recruitment reference for every OPR phase-placement comparison.

