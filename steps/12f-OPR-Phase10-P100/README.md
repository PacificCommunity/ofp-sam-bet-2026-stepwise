# 12f OPR switch at Phase 10, P100

Runs standard PDH-rebuild controls through the SRR phases (Phase 9), then
activates OPR in the `09.par → 10.par` call and applies `pf397=100` in
Phase 11.

This is the core late-switch terminal-penalty candidate.

