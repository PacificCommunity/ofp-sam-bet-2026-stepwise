# 11a Standard terminal-free control

Its `doitall.sh` rebuilds Step 11 from the initial inputs, then refits standard
recruitment with `pf398=0` and `pf400=0`. This isolates terminal recruitment
constraints before comparing OPR models.

It writes a fresh `11.par` and then `final.par`.
